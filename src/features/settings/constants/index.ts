export type NotificationSettings = {
  dustAlert: boolean
  activityForecast: boolean
  emergencyAlert: boolean
}

export type SettingsCardKey = 'sensitive' | 'activity' | 'places' | 'temperature' | 'score'
